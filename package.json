{
    "name": "horizon",
    "version": "1.0.0",
    "description": "Stuff Horizon Server Workaround",
    "engines": {
        "node": "4.3.2"
    },
    "postinstall": "for dir in server ; do printf \"=== Installing %s\\n\" \"$dir\" ; ( cd \"$dir\" ; npm install ; ) ; printf \"=== Installation exit code: %s for %s\\n\" $? \"$dir\" ; done "
}
